struct Leg - структура содержащая одно поле / пользовательский тип
{
    int length; - тип инт для целых чисел
};

struct Arm - структура с одним полем
{,
    int power; - тоже инт
};

struct Person - структура содержащая
{
    std::array<Leg, 2>(это все тип)    legs; - массив из двух элементов типа Лег (стд… - массив фиксированный размер)/(этот массив создает 2 эл Легс типа инт. но также создает массив содержащий 2 объекта\это )

    Arm arms[2]; - массив из двух элементов типа Арм/не объект, элементы не объединены, но сами элементы
};

int main()
{
    Person(тип)  person; - создаем объект персон с данными из структуры Персон
}

объекты -  person, (leg[0]  leg[1] - legs - содержится в персон и считается объектом из-за этого),  (arm[0] arm[1]) (int length\int power)
типы - int,  std::array<Leg, 2>, Leg, Arm, Person
